ASSIGNMENT ID: I4-explorer (EXPLORER TAB)
FILES YOU OWN: src/client/tabs/explorer/** (tab-def.ts, events.ts, state.ts, ExplorerPanel.tsx, TreeNodeRow.tsx, ExplorerPanel.module.css, locales.ts), tests/client/tabs/explorer/**

Design docs: d2-explorer.md (primary; NOTE sections 1-6 were truncated in the file — the ADRs carry the missing decisions), adr-004-explorer-git.md, adr-003 (tab contract). d8-testing.md section 3.5.

Seams ALREADY written (keep): src/client/tabs/explorer/events.ts (ExplorerOpenFileEvent, ExplorerEvents, ExplorerOpenFileEmitter — full implementation, use it), src/client/tabs/explorer/tab-def.ts (STUB — REPLACE with the real factory), src/client/tab-registry/contract.ts (TabDef), src/client/rpc-client.ts (BetterSidebarRpc), src/client/dock/context.ts (useDock: { rpc, useSessions, useWorkspaces }), src/client/icons.tsx (FolderIcon, FileIcon, SymlinkIcon, ChevronRightIcon, ChevronDownIcon, RefreshIcon), src/client/workspace-root.ts (resolveRoot(sessions, workspaces)).

Implement:
1. src/client/tabs/explorer/state.ts — ExplorerStore: a plain observable store (listener set + getSnapshot or simple class) holding: root (string | undefined), surface phase ('no-workspace'|'loading'|'loaded'|'root-error' + error), node map: Record<path, NodeState> where NodeState = { entry, expanded, children?: ExplorerEntry[] (loaded), loadState: 'idle'|'loading'|'error'|'loaded', loadError? }, expandedPaths, selectedPath, focusedPath, rootGen counter. Actions: setRoot(path), loadRoot(), expand(path) (lazy load children via injected loader fn), collapse(path), toggle(path), refresh() (re-list expanded dirs), select(path), focus(path). Stale-response guards: per-node request seq + rootGen (increment on root change; discard stale async results). Collapse does NOT unload children. prunePath(path). Pure logic, no React.
2. src/client/tabs/explorer/ExplorerPanel.tsx — the panel: uses useDock() for rpc + hooks; resolves root via resolveRoot(useSessions(s=>s), useWorkspaces(w=>w)) with the standard hook selectors; renders surface states: no-workspace (message + hint), loading (spinner row), root-error (message + retry), loaded (tree). Tree renders visible nodes recursively from the root children; dirs have a caret (ChevronRight/Down) toggling expand; rows: icon by kind (Folder/File/Symlink), name, symlink indicator, per-node inline error with retry on load failure; refresh button in panel header; open-file: Enter/double-click on file rows -> emitter.emit({ path, name, kind:'file', source, rootPath }); selection + focus per D2 section 9 (roving tabindex, aria tree semantics: role=tree/treeitem/group, aria-expanded/selected, keys ArrowUp/Down/Left/Right/Home/End/Enter/Space/*; Delete unbound). NOTE: includeHidden toggle is DEFERRED (contract request has no flag; host always filters) — do NOT add a broken toggle; document in code + report.
3. src/client/tabs/explorer/TreeNodeRow.tsx — one row component (props: entry, depth, expanded, selected, focused, loadState, handlers).
4. src/client/tabs/explorer/ExplorerPanel.module.css — row styles, indent via padding-left = depth, caret/icon sizing, hover, selected/focused backgrounds using var(--bsd-*) tokens.
5. src/client/tabs/explorer/tab-def.ts — REPLACE stub: export function createExplorerTabDef(ctx: ClientContext, api: { rpc: BetterSidebarRpc; emitter: ExplorerOpenFileEmitter }): TabDef — returns { id:'explorer', order:10, label: () => t('tabLabel'), icon: <FolderIcon/>, renderPanel: () => <ExplorerPanel rpc={api.rpc} emitter={api.emitter} /> } (prefer explicit props over useDock for rpc — document).
6. src/client/tabs/explorer/locales.ts — namespace 'betterSidebar.explorer' with en+zh keys: tabLabel, noWorkspace, noWorkspaceHint, loading, retry, refresh, rootDeleted, loadFailed, expand, collapse, openFile. Export { NS, en, zh } (the client-core index.ts imports and registers them). LocaleNamespaceMap merge in this file.

TESTS (tests/client/tabs/explorer/, jsdom; d8-testing.md):
- state.spec.ts — store transitions: root set, expand loads children (fake loader), seq guard (out-of-order responses discarded), rootGen guard, collapse keeps children, prune, refresh re-lists, error node state.
- explorer-panel.spec.tsx — render ExplorerPanel inside DockContext.Provider with stub rpc (fake rpc.call returning canned SidebarResult values) + stub hooks; cover: no-workspace state, root-error + retry, loaded tree (rows + expand -> children appear), refresh button, open-file emission (spy on emitter), a11y roles (tree/treeitem) and keyboard: ArrowDown moves focus, Enter on file emits open event, * expands one level.
- tab-def.spec.ts — factory returns the expected TabDef shape (id/order/label/icon/renderPanel renders).
Do NOT use the real dsh test-runtime for tab tests (keep them framework-free with DockContext.Provider).