ASSIGNMENT ID: I3-client-core (CLIENT CORE)
FILES YOU OWN: src/client/index.ts, src/client/tab-registry/service.ts, src/client/dock/** (dock.tsx, dock-store.ts, dock.module.css, rail.tsx, rail.module.css, resize.ts), src/client/tabbar/** (tablist.tsx, tabpanel.tsx, tablist.module.css), src/client/locales.ts, src/client/styles.css, tests/client/dock-*.spec.tsx, tests/client/tab-registry.spec.ts, tests/client/rpc-client.spec.ts, tests/client/workspace-root.spec.ts, tests/client/plugin-apply.spec.tsx
KEEP (do not modify): src/client/rpc-client.ts, src/client/dock/context.ts, src/client/icons.tsx, src/client/workspace-root.ts, src/client/tab-registry/contract.ts, src/client/tabs/explorer/tab-def.ts (stub, other task owns), src/client/tabs/git/tab-def.ts (stub, other task owns), src/client/tabs/explorer/events.ts (full, other task owns).

Design docs: d7-client-ui.md (primary), d4-tab-registry.md, d1-architecture.md sections 3-5. ADRs 001/002/003.

Implement:
1. src/client/tab-registry/service.ts — class BetterSidebarTabRegistry implements the contract: ordered list by (order ?? 1000, registration index), duplicate id throws TabRegisterError, register returns idempotent disposer, subscribe/notify, active-tab with localStorage persistence (key 'dsh.betterSidebar.activeTab': on select/register; on unregister of the active tab fall back to first remaining; corrupt/stale value tolerated), select(id) returns false for unknown id.
2. src/client/dock/dock-store.ts — useSyncExternalStore store for { open, width } seeded from localStorage 'dsh.betterSidebar.dock' (JSON {open,width}), default open:true width:320; clamp [260,560]; actions setWidth/open/close/toggle; rail width 48. No window globals at module scope (read localStorage lazily).
3. src/client/dock/resize.ts — pointer-based drag helper: onPointerDown on the left-edge handle, track pointermove, fire onWidth(dx), pointerup ends; touch-action:none.
4. src/client/dock/dock.tsx — DockRoot: the shell.overlay entry component. Receives global-standard props (useSessions, useWorkspaces) plus rpc + tabs as props (the plugin entry passes them: register({ name:'shell.overlay', id:'better-sidebar', order:0 }, (props) => <DockRoot {...props} rpc={rpc} tabs={tabs} />)). Provides DockContext.Provider value { rpc, useSessions, useWorkspaces }. Renders: rail when collapsed or narrow (window.innerWidth < 900 via a resize listener), else full dock: header (title + collapse button), TabBar (registry snapshot: ids to tabs, active highlight, click select, arrow keys), TabPanel (active tab's renderPanel() inside the DockContext.Provider). aria: role=region + label, tablist/tab/tabpanel, roving tabindex.
5. src/client/dock/rail.tsx — collapsed rail: vertical column of tab icon buttons (48px), click -> select + expand.
6. src/client/tabbar/tablist.tsx + tabpanel.tsx — small presentational components (tablist: buttons with icon+label, aria-selected, roving tabindex, arrow key handling; tabpanel: role=tabpanel aria-labelledby, renders children).
7. src/client/locales.ts — namespace 'betterSidebar.dock' (declare module '@deepseek-ai/dsh-client-locale/client' LocaleNamespaceMap), en + zh dictionaries for: dock title (Right sidebar), collapse/expand labels, refresh, explorer tab label, git tab label, empty/loading generic copy. Export NS constant + dictionaries.
8. src/client/styles.css — :root --bsd-* tokens (bg, border, text, muted, accent, danger), dark variant via prefers-color-scheme + body[data-ds-dark-theme] attribute (MutationObserver), color-scheme.
9. src/client/index.ts — plugin entry: inject ['connection','slots','locale']; apply(ctx): ctx.effect(() => { const rpc = createBetterSidebarRpc(ctx.connection); const tabs = new BetterSidebarTabRegistry(); const emitter = new ExplorerOpenFileEmitter(); provide ctx.betterSidebar = { rpc, tabs, explorer: emitter } via ctx.reflect.provide inside the effect (verify the pattern in packages/client/ui-layout/src/client/index.ts line ~118); register locale dicts (ctx.locale.register for 'betterSidebar.dock' AND import+register the dictionaries exported by src/client/tabs/explorer/locales.ts and src/client/tabs/git/locales.ts — those files are owned by other tasks but MUST exist with those exact exports (NS, en, zh); coordinate via your report if they are missing when you integrate); register built-in tabs: tabs.register(createExplorerTabDef(ctx, { rpc, emitter })) and tabs.register(createGitTabDef(ctx, { rpc })) — dispose in teardown; register the dock entry into 'shell.overlay' (id 'better-sidebar', order 0); global toggle shortcut Ctrl/Cmd+Shift+B (keydown listener dispatching a CustomEvent 'better-sidebar:toggle' on window; DockRoot listens). Export types (BetterSidebarService etc.). declare module '@deepseek-ai/cordis' { interface Context { betterSidebar: BetterSidebarService } }. Import type {} from '@deepseek-ai/dsh-client-ui-layout/client' for the shell.overlay SlotMap merge (brief section 7.4).
   Service face: export interface BetterSidebarService { rpc: BetterSidebarRpc; tabs: BetterSidebarTabRegistry; explorer: ExplorerEvents }.

TESTS (tests/client/, jsdom; d8-testing.md section 3):
- tests/client/tab-registry.spec.ts — register/unregister/dispose/order/duplicate-throw/active fallback/localStorage persistence.
- tests/client/rpc-client.spec.ts — fake connection.rpc.call: success value envelope, domain error in value, RPC error slot cancelled, rejected call -> internal transport error, aborted signal -> cancelled.
- tests/client/workspace-root.spec.ts — precedence: session cwd > single workspace > recentWorkspaceId > undefined.
- tests/client/dock-shell.spec.tsx — render DockRoot with stub rpc + stub useSessions/useWorkspaces + a real registry with fake tabs; cover: default open renders tablist + panel; collapse -> rail; tab switch activates panel; resize via pointer events on handle (user-event pointer lifecycle); narrow window -> rail-only (mock window.innerWidth).
- tests/client/plugin-apply.spec.tsx — use the dsh test-runtime (SlotTestRuntime from '@deepseek-ai/dsh-client-test-runtime', see tests/client/test-runtime-probe.spec.tsx for the exact pattern incl. the ui-layout/client type import): create runtime, provide 'connection' with a fake ConnectionHandle (rpc.call capturing), declare { 'shell.overlay': { kind:'list', scope:'root' } }, mount our plugin (import from src/client/index.ts), render root, assert: the dock renders (rail or dock), ctx.betterSidebar exists (read via the runtime ctx), locale registration happened. NOTE: the tab-def factories may still be stubs that throw at registration if the tab tasks have not landed — for the apply test, mount a REDUCED entry function defined in the test that replicates apply's non-tab wiring (dock entry + service + locales), and assert that path; also assert that the real apply registers tabs once they exist. Document the dependency in your report.